@* TODO: `@page` は最初の Razor ディレクティブにします *@
@page
@* 画面と対応するクラスを指定する *@
@* <p>HelloModel</p>で使えるのかと思ったらこのクラスの使い方わからなかった *@
@model RazorPagesMovie.Pages.Practice.HelloModel
@{
    ViewData["Title"] = "Practice";
}


<h1>Razor Page の練習</h1>

<p>こんちか。いちかでっす。</p>
<p>最初のRazor Pageでっす。</p>

<p>最近のお気に入りは、つめた～い麦茶でっす。</p>

@* ここでC# からデータを渡す *@

@* CSからメッセージを表示する *@
@* Modelは`@model`で指定した値が使われる *@
<p>メッセージ： @Model.Message</p>
@* FormatProviderというものがありました。ここでフォーマットで指定できる *@
@* ここがリロードごとに更新されるってことは、OnGetがその都度動いているってこと *@
<p>現在の時刻： @Model.DisplayAt.ToString("yyyy/MM/dd, HH:mm:ss")</p>

